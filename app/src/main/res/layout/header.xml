<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:orientation="vertical"
    android:gravity="bottom"
    android:paddingLeft="15dp"
    android:paddingBottom="15dp">

    <androidx.cardview.widget.CardView
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_marginLeft="0dp"
        app:cardCornerRadius="40dp">

        <ImageView
            android:id="@+id/imgheader"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:layout_marginLeft="0dp"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/lg" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/lblhradername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/frank_ruhl_libre_medium"
        android:text="Home Comfort"
        android:textColor="#090909"
        android:textSize="25sp"
    />

    <TextView
        android:id="@+id/lblheaderemail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/playfair_display_italic"
        android:text="We are just one step away"
        android:textColor="#000000"
        android:textSize="15sp" />

</LinearLayout>